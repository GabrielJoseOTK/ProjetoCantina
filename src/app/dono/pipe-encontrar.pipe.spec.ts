import { PipeEncontrarPipe } from './pipe-encontrar.pipe';

describe('PipeEncontrarPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeEncontrarPipe();
    expect(pipe).toBeTruthy();
  });
});
